{
  "name": "workshop",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@workshop/client": "workspace:client",
    "express": "4.19.2"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "start": "node server/index.js",
    "build": "pnpm --filter @workshop/client build",
    "test": "pnpm --filter @workshop/client test"
  },
  "engines": {
    "node": ">=20.11",
    "npm": ">=8.0.0",
    "pnpm": ">=8.0.0"
  }
}
